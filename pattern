#include<stdio.h>
#define MAX 5
int main()
{
    for(int i=1;i<=MAX;i++)
    {
        for(int j=1;j<=i;j++)
        {
            printf("%d",j);
        }
        printf("\n");
    }
    return 0;
}
